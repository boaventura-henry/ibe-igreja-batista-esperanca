"use client";
import { useState } from "react";
import type { PortalScheduleRepertoire as Repertoire } from "@/types";
type Api<T> = { success: true; data: T } | { success: false; error: { message: string } };
export function PortalScheduleRepertoire({ scheduleMemberId }: { scheduleMemberId: string }) { const [data, setData] = useState<Repertoire | null>(null); const [open, setOpen] = useState(false); const [message, setMessage] = useState(""); async function load() { const response = await fetch(`/api/my-schedules/${scheduleMemberId}/repertoire`, { cache: "no-store" }); const payload = await response.json() as Api<Repertoire>; if (payload.success) { setData(payload.data); setOpen(true); } else setMessage(payload.error.message); } return <div className="mt-2"><button type="button" onClick={() => void load()} className="rounded-md border border-hope-100 px-3 py-2 text-xs font-bold">Ver repertorio</button>{message ? <p className="mt-1 text-xs font-semibold text-ink-600">{message}</p> : null}{open ? <div className="mt-3 rounded-md bg-hope-50 p-3"><div className="flex items-center justify-between"><p className="text-sm font-bold">Repertorio</p><button type="button" onClick={() => setOpen(false)} className="text-xs font-bold">Fechar</button></div>{data?.songs.length ? <ol className="mt-2 grid gap-2 text-sm">{data.songs.map((item) => <li key={item.id}><span className="font-semibold">{item.position}. {item.song.title}</span>{item.performanceKey ? ` - ${item.performanceKey}` : ""}{item.leadMember ? ` | ${item.leadMember.name}` : ""}<div className="flex flex-wrap gap-2 text-xs">{item.youtubeUrlOverride || item.song.youtubeUrl ? <a target="_blank" rel="noopener noreferrer" href={item.youtubeUrlOverride || item.song.youtubeUrl || "#"} className="text-hope-700">YouTube</a> : null}{item.resourceUrlOverride || item.song.resourceUrl ? <a target="_blank" rel="noopener noreferrer" href={item.resourceUrlOverride || item.song.resourceUrl || "#"} className="text-hope-700">Material</a> : null}{item.useSimplifiedVersion ? <span>Versao simplificada</span> : null}</div></li>)}</ol> : <p className="mt-2 text-sm text-ink-600">Esta escala ainda nao possui repertorio.</p>}</div> : null}</div>; }
